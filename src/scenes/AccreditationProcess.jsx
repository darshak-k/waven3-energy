import OpenCards from "./HozizontalScrollCorousel";

const AccreditationProcess = () => {
  return (
    <section id="skills">
      {/* HEADER AND IMAGE SECTION */}
      <OpenCards />
    </section>
  );
};

export default AccreditationProcess;
